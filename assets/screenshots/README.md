# Screenshots

Place dashboard screenshots here, referenced from the root `README.md`.

Suggested shots (PNG, ~1280px wide, capture the dashboard in a browser tab):

- `dashboard-dark.png` — full dashboard in dark mode
- `dashboard-light.png` — full dashboard in light mode (shows theme support)
- `golden-hours.png` — close-up of the 7-day golden focus hours heatmap
- `rankings.png` — daily/weekly app rankings with the category legend open

Keep file sizes reasonable (compress before committing). Once added, the
placeholders in the root README's "Screenshots" section will render.
