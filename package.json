{
  "name": "timewise",
  "version": "1.0.1",
  "description": "Privacy-first, local-only browser time tracker",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "zip": "node scripts/build.js && cd build && powershell -Command \"Compress-Archive -Path timewise\\* -DestinationPath timewise-v$npm_package_version.zip -Force\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mikewoo/TimeWise.git"
  },
  "license": "MIT"
}
